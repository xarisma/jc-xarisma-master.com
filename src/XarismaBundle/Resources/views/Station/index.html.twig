{% extends 'XarismaBundle:Default:base.html.twig' %}

{% block body -%}
    <h1>Work Station List</h1>

    <table class="records_list table table-hover">
        <thead>
            <tr>
                <th>Id</th>
                <th>Code</th>
                <th>Description</th>
{#                <th>Actions</th>#}
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('station_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.stationcode }}</td>
                <td>{{ entity.description }}</td>
{#                <td>
                <ul>
                    <li>
                        <a href="{{ path('station_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('station_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>#}
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('station_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
