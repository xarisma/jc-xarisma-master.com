{% extends 'XarismaBundle:Default:base.html.twig' %}

{% block body -%}
    <h1>Dictionary List</h1>

    <table class="records_list table table-hover">
        <thead>
            <tr>
                <th>Id</th>
                <th>Class</th>
                <th>Term</th>
                <th>Definition</th>
{#                <th>Actions</th>#}
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('dictionary_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.class }}</td>
                <td>{{ entity.term }}</td>
                <td>{{ entity.definition }}</td>
{#                <td>
                <ul>
                    <li>
                        <a href="{{ path('dictionary_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('dictionary_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>#}
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('dictionary_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
